add ax,120h
mov cx,320h
mov dx,520h
add dx,ax
mov bx,450h
add dx,bx
mov ax,2
mov cx,4 
sub cx,ax
mov cx,3  
add ax,120h
mov cx,320h
mov dx,520h
add dx,ax
mov bx,450h
add dx,bx
mov ax,2
mov cx,4 
sub cx,ax
mov cx,3